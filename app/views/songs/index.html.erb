<a href="/songs">Dashboard</a>

  <h3>Others who added <%= @song.artist %> - <%= @song.title %></h3>
  <table>
    <tr>
      <th>Name</th>
      <th>Times Added</th>
    </tr>
    <tr>
  <% @song1.each do |user| %>
      <td><a href="/users/<%= current_user.id %>"><%= user.first_name %><%= user.last_name %></a></td>
      <td><%= user.users_count %></td>

      </td>
    </tr>
    <% end %> 
  </table>
